// ===================================================
// section-base.js：基本情報セクション
// 役割：サイト名・運営者・連絡先・所在地などの基本情報を出力。
// ===================================================
export default function (data) {
  const b = data.base || {};
  return `
<section id="base">
  <h2>基本情報</h2>
  <p>本プライバシーポリシーは、${b.siteName || '本サイト'}（以下、「当サイト」）が運営するウェブサイトにおいて、利用者の個人情報を適切に取り扱うための方針を定めたものです。</p>
  <ul>
    <li>運営者名：${b.operatorName || '（運営者名）'}</li>
    <li>代表者名：${b.representative || '（代表者名）'}</li>
    <li>所在地：${b.address || '（住所）'}</li>
    <li>お問い合わせ先：${b.contactEmail || '（メールアドレス）'}</li>
  </ul>
</section>`;
}
